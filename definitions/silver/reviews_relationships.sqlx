config {
  type: "assertion",
  schema: "olist_assertions",
  name: "reviews_valid_orders",
  tags: ["phase_10", "quality"]
}

SELECT
  r.review_id,
  r.order_id

FROM ${ref("reviews")} AS r

LEFT JOIN ${ref("orders")} AS o
  ON r.order_id = o.order_id

WHERE o.order_id IS NULL